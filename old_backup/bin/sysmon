#!/data/data/com.termux/files/usr/bin/bash
# Launcher para SysMon (Python)

cd ~/Code/sysmon

echo "🚀 Iniciando SysMon Python..."
echo "📁 Diretório: $(pwd)"

# Configura PYTHONPATH
export PYTHONPATH="$(pwd):$PYTHONPATH"

# Instala dependências se necessário
if [ -f "sysmon/requirements.txt" ]; then
    echo "📦 Verificando dependências..."
    pip install -r sysmon/requirements.txt 2>/dev/null || echo "⚠️  Não foi possível instalar automaticamente"
fi

# Executa
echo "⚡ Executando sysmon..."
python -m sysmon.main
